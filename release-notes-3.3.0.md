# AI Classroom 3.3.0

## 对话模型与推理控制

- 关闭“深度思考”时，请求体不会发送 `reasoning_effort` 等推理参数，普通模型不会被意外启用思考模式。
- 开启“深度思考”后，对话输入栏上方提供 `low / medium / high` 三档推理强度滑块。
- 对话界面可随时切换已配置的备选模型，模型选择按课堂保存，并会优先用于当前课堂的后续请求。
- 模型与推理控制收纳为轻量图标，只有点击时才显示选择菜单，减少输入区常驻占用。
- 新增沉浸式对话模式，可隐藏顶部标题栏和底部导航，仅保留对话及输入区域。
- 沉浸模式提供快速明暗切换按钮，无需离开当前对话进入设置页。
- 兼容旧课堂数据：新增配置字段自动使用安全默认值。

## 兼容性

- 长时世界书、知识库检索、章节记忆和不同人格/视角隔离逻辑保持不变。
- 后台的课堂命名、记忆整理和分支对话继续沿用课堂模型配置与回退链。
