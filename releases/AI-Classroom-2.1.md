# AI Classroom 2.1

## 更新内容

- 修复皮肤效果失效问题，导航栏、按钮、输入框、筛选项和卡片统一跟随当前皮肤。
- 设置页改为“最多展开一个模块”，保存后自动收起当前模块。
- 删除独立 API 模块，模型模块、多模态模块、TTS 模块分别配置自己的 Base URL、API Key 和模型。
- 多模态图片分析会优先使用多模态模块的独立 API 配置。
- 优化主课堂图片按钮位置，避免压在输入框边线或挤压发送按钮。
- 更新首次打开 New 弹窗、设置页使用手册和 README。

## APK

- `ai-classroom-2.1-debug.apk`
