# AI Classroom 3.1.0

## 小说写作知识库升级

- 普通知识库改为按当前问题、最近对话和课堂主题进行轻量检索，不再按上传顺序把整库粗暴截断。
- 资料会按段落切分，只召回少量相关片段，降低长资料占满上下文、后半段完全失效的问题。
- 主课堂和分支课堂都支持本轮资料检索。
- 知识库文件列表继续支持点击查看全文、长按删除和本地持久化。

## Markdown 角色卡

- 新增“导入角色卡”入口，支持 `.md` 文件。
- 角色卡会自动读取 Markdown 一级标题作为角色名。
- 角色卡作为作者级人物参考持续参与小说创作，用于保持人物身份、性格、语言风格、动机、关系和行为边界的一致性。
- 角色卡不会自动让角色获得其他角色的秘密；角色仍只能依据自身可见事实和已知信息行动。
- 最近连续对话中已经发生的事实优先于旧角色卡设定，避免剧情更新后被旧资料覆盖。

## 长期连续性与兼容性

- 保留世界书、章节索引、长期记忆卡、人格视角、私密记忆可见性、分支课堂、多模型 fallback、Vision、TTS 和考试工具。
- 旧版本知识库文件继续可读取，旧文件会自动识别为普通资料。
- 新增的资料类型字段会随课堂数据本地保存，并兼容旧课堂 JSON。

## 关于“接近微调”的说明

角色卡常驻、普通资料检索、世界书、长期记忆和最近对话优先级组合后，可以在单个小说项目中接近微调后的设定一致性与连续性，但不会修改模型参数，也不会永久改变模型本身的写作能力。

建议每位主要角色单独建立一张 Markdown 角色卡，把世界规则写入世界书，并额外保存 2—3 段期望的叙事示例作为普通资料，以获得更稳定的小说创作效果。

## 其他

- 版本号：`3.1.0`
- APK 使用本地 AI Classroom 开发者证书签名。
- 默认界面继续保持简约、扁平、低饱和灰蓝风格，避免过粗线条和多层装饰。
